-- =============================================
-- TeaNote v85 マイグレーション
--   評価カードのハガキサイズ変換（印刷用）機能を追加
-- Supabase SQL Editor で実行してください
-- =============================================
--
-- コンビニのカラー印刷では名刺サイズを直接印刷できないため、
-- 評価カード2枚をハガキサイズ（100×148mm）1枚にまとめる機能。
--
-- 開発中のため消費ポイントは 0（無料）で登録する。
-- 有料化する場合は、管理画面「💎 ポイント設定」から
-- この機能のコストを変更すれば、コード修正なしで反映される。

insert into public.feature_costs (feature, cost, label, sort_order) values
  ('card_print', 0, '印刷用ハガキサイズ変換', 5)
on conflict (feature) do nothing;
